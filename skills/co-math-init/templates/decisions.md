# Decisions log — {{PROJECT_NAME}}

> Append-only. Most recent on top. Every meaningful project decision is
> recorded here, with timestamp and rationale, so future sessions (and the
> reviewer agents) can reconstruct *why* the project ended up where it did.

---

## {{DATE}} — Project initialized

- Research question recorded in `goals.md`.
- Strict mode: see `co-math-config.json`.
- No workstreams yet.
- Goals not yet approved by user. The project-coordinator must not spawn
  workstreams until `goals.md` `Approval:` line reads `YES`.
